// TODO
// import pdfReader from '@app/utils/third-party/pdf-reader';

// const { PDFViewerApplication, AppOptions } = pdfReader;

export default defineNuxtPlugin(() => {

  return {
    provide: {
      // pdfViewer:PDFViewerApplication,
      // pdfViewerAppOptions: AppOptions
    }
  };
});
